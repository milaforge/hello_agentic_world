# Current stage: 5 — Governed Agent

`main` contains the final cumulative implementation.

To learn from the beginning, start from `v0` and follow [`docs/CURRICULUM.md`](docs/CURRICULUM.md). Each `stage/*` branch replaces this file with the task, constraints, and pass criteria for that stage.
